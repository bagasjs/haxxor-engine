#include "Texture.h"

namespace Haxxor
{
    Ref<Texture> Create(const std::string& filepath)
    {
        return nullptr;
    }

    Ref<Texture> Create(uint32_t width, uint32_t height, const uint8_t* data)
    {
        return nullptr;
    }
}